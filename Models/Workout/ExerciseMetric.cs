namespace FocusFlowFinal.Models.Workout;

public enum ExerciseMetric
{
    WeightReps,  // кг + повторения (силовые)
    RepsOnly,    // только повторения (своё тело)
    TimeOnly,    // только время сек (кардио/планка)
    TimeReps     // время + повторения
}
